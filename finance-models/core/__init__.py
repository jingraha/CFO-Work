"""Core workbook-generation engine for the Startup CFO OS Excel model track.

This package contains reusable, model-agnostic building blocks (formatting,
layout helpers, sheet builders) used by every business-model workbook under
``finance-models/models``. Nothing in this package should hard-code numbers
that belong to a specific business model - those live in ``models/*.py``.
"""
